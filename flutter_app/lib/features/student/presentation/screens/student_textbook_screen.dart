import 'package:flutter/material.dart';

class StudentTextbookScreen extends StatelessWidget {
  const StudentTextbookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Textbooks'),
      ),
      body: const Center(
        child: Text('Student Textbook Screen - Coming Soon'),
      ),
    );
  }
}

