import 'package:flutter/material.dart';

class DashboardPostsScreen extends StatelessWidget {
  const DashboardPostsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dashboard Posts'),
      ),
      body: const Center(
        child: Text('Dashboard Posts Screen - Coming Soon'),
      ),
    );
  }
}

